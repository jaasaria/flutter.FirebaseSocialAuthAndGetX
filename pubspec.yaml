name: getx_main_example
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  get: ^4.1.4
  firebase_core: ^1.2.0
  firebase_auth: ^1.2.0
  cloud_firestore: ^2.2.0
  flutter_form_builder: ^6.0.0
  textless: ^6.6.6
  lint: ^1.5.3
  google_sign_in: ^5.0.3
  flutter_login_facebook: ^1.0.1
  twitter_login: ^3.0.8
  json_annotation: ^4.0.1
  social_auth_buttons: ^2.0.0-nullsafety.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.0.3
  json_serializable: ^4.1.2

flutter:
  uses-material-design: true
